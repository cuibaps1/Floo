<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.quidproquo.floo">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".Startup.StartupScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Home.HomeScreenActivity" />
        <activity android:name=".Login.LoginActivity" />
        <activity android:name=".Register.RegisterActivity" />
        <activity android:name=".Register.InformationActivity" />
        <activity android:name=".Register.LanguagueActivity" />
        <activity
            android:name=".HomeScreenBottomNavigationBar"
            android:label="@string/title_activity_home_screen"></activity>
        <activity android:name=".Chat.ChatActivity"/>
        <activity android:name=".Profile.ProfileActivity"/>
        <activity android:name=".Profile.AboutMeActivity"/>
        <activity android:name=".Profile.LanguageActivity"/>
        <activity android:name=".Profile.PreviewProfileActivity"/>

    </application>

</manifest>